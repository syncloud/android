apply plugin: 'java'

dependencies {
    compile project(':ssh')
    compile project(':insider')
    testCompile 'junit:junit:4.10'
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

test {
    exclude "org/syncloud/remote/integration/**"
}
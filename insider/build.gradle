apply plugin: 'java'

dependencies {
    compile project(':ssh')
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.10'
}

test {
    exclude "org/syncloud/insider/integration/**"
}
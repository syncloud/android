apply plugin: 'java'

dependencies {
    compile project(':ssh')
    compile 'com.google.guava:guava:16.0.1'
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.10'
}

test {
    exclude "org/syncloud/apps/remote/**"
}
apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.fasterxml.jackson.core:jackson-databind:2.3.3'
}

jacocoTestReport() {
    reports {
        xml.enabled false
        csv.enabled true
    }
}